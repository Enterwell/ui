import { useDebounce } from '@enterwell/hooks';
import { ComponentDescription, ComponentParameters } from '../../../components/docs/ComponentDocs';

# useDebounce

## Description

<ComponentDescription name="useDebounce" />

### Parameters

<ComponentParameters name="useDebounce" />

## Example

```ts filename="example.ts" {4}
import { useDebounce } from '@enterwell/hooks';

const [value, setValue] = useState(0);
const valueDebounced = useDebounce(value, 50);
```